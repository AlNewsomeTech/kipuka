{
  "name": "SecurityReviewNote",
  "type": "object",
  "description": "Dated Pac-Sec analyst review notes for an ACOLYTE-serviced project, visible to the client org.",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "project_id": {
      "type": "string"
    },
    "note_date": {
      "type": "string",
      "format": "date"
    },
    "author": {
      "type": "string",
      "description": "Pac-Sec analyst name/email."
    },
    "title": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "visibility": {
      "type": "string",
      "enum": [
        "Client Visible",
        "Internal Only"
      ],
      "default": "Client Visible"
    }
  },
  "required": [
    "project_id",
    "title"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "technician"
          }
        },
        {
          "user_condition": {
            "role": "client"
          }
        }
      ]
    },
    "write": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "technician"
          }
        }
      ]
    }
  }
}