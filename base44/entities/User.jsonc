{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "technician",
        "client"
      ]
    },
    "assigned_client_ids": {
      "type": "string",
      "description": "Comma-separated client IDs assigned to this user (for technicians and clients)"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}